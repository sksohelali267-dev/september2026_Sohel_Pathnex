# Day 24 — Advanced Terraform Modules and CI/CD Pipeline Scaling

## Ansible — Deploy Docker Containers with Kubernetes Using Helm

```yaml
- name: Deploy Docker Containers with Helm
  hosts: localhost
  tasks:
    - name: Install Helm
      command: curl https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3 | bash
    - name: Deploy microservices with Helm
      command: helm install sohel-app ./microservice-chart


module "sohel_vpc" {
  source = "./vpc"
}

module "sohel_instance" {
  source = "./instance"
  vpc_id = module.sohel_vpc.vpc_id
}

output "instance_id" {
  value = module.sohel_instance.instance_id
}


apiVersion: apps/v1
kind: Deployment
metadata:
  name: sohel-web-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: sohel-web-app
  template:
    metadata:
      labels:
        app: sohel-web-app
    spec:
      containers:
        - name: nginx
          image: nginx
          ports:
            - containerPort: 80
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sohel-web-app-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sohel-web-app
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80

pipeline {
    agent any
    stages {
        stage('Scale Up') {
            steps {
                echo 'Scaling up Jenkins pipeline agents for heavy workloads...'
                // Simulating scaling logic
            }
        }
        stage('Build') {
            steps {
                echo 'Building project...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying project...'
            }
        }
    }
}

# Healthcheck

FROM nginx
HEALTHCHECK CMD curl --fail http://localhost || exit 1
